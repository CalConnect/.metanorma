# CalConnect Metanorma Configuration

Channel definitions and default routing rules for CalConnect document repos.

## Structure

- `.metanorma/channels.yml` — Default channel config (standards, directives, reports, etc.)

Additional configs can be added as `.metanorma/{name}.yml` for subunits.
